name: admin_panel
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: 2.5.9
  go_router:
  shared_preferences:
  shared_preferences_web:
  form_validator: 
  loading_animation_widget:
  font_awesome_flutter:
  intl:
  file_picker: ^8.1.4
  flutter_dotenv:
  flutter_bloc:
  dartz:
  equatable:
  flutter_secure_storage:
  get_it:
  connectivity_plus:
  easy_localization:
  provider: ^6.1.5+1
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  cached_network_image:
  image: ^4.5.4
  uuid:
  qr_flutter:
  timeago: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  bloc_test: ^9.1.5
  mocktail: ^1.0.3
  integration_test:
    sdk: flutter
  faker: ^2.1.0 
  golden_toolkit: ^0.15.0
  build_runner: ^2.4.6
  build_test: ^2.2.0

# ✅ CRITICAL: Lock web package to compatible version
# This prevents transitive dependencies from pulling incompatible versions
# ✅ CRITICAL FIX: Force exact compatible versions
dependency_overrides:
  web: 0.5.1
  js: 0.6.7

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/logo/
    - assets/translations/